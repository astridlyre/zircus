include: [_headers]

home_link_path: '/'
home_link: 'Zircus Home Page'

collections:
  products:
    output: true

defaults:
  - scope:
      path: ""
      type: "products"
    values:
      layout: "product"
