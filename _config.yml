---
include: [_headers, _redirects]

name: "zircus"

collections:
  products:
    output: true

sass:
  style: compressed

defaults:
  - scope:
      path: ""
      type: "products"
    values:
      layout: "product"
